// import * as React from 'react';
// import * as ReactDOM from 'react-dom';
// import * as TestUtils from 'react-dom/test-utils';

// import Contact, { ContactState, FormValid } from '../contact';
// import { constants } from 'os';

// describe('Contact Form Test', () => {
//     const contact: React.Component = TestUtils.renderIntoDocument(
//         <Contact />
//     ) as React.Component;

//     it('Contact state는 name, phone, email, message를 가지고 있어야 한다.', () => {
//         let regExp: RegExp = /name|phone|email|message/;
//         for (let key in contact.state) {
//             console.log(key);
//             expect(key).toMatch(regExp);
//         }
//         contact.getSnapshotBeforeUpdate;
//     });
// });
